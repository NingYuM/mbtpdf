# Agent TODOs

- [x] Investigate LZW large-payload decode/encode stability around code-length transitions (see TODO in `codec/pdfcodec/pdfcodec_coverage_extra_test.mbt`).
- [x] Review and either apply or drop the existing git stash: `stash@{0}` (dropped).
